httpx>=0.27.0
requests>=2.31.0
python-dotenv>=1.0.0
# Rendered signal/cover charts (image graphics). If install fails or is removed,
# the bot degrades automatically to text-only cards (ENABLE_CHARTS has no effect).
matplotlib>=3.7.0
